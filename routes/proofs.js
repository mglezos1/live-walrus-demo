// routes/proofs.js
import express from "express";

const router = express.Router();

// Proof routes are handled directly in app.js
// This file exists for future route expansion

export default router;
